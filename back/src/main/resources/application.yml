spring:
  datasource:
    url: jdbc:mysql://localhost:3306/message?useSSL=false&serverTimezone=UTC
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
        dialect: org.hibernate.dialect.MySQL8Dialect

client:
  url: http://localhost:3000